#include <stdio.h>

/**
 * main - Prints all single digit numbers of 10 starting from 0,
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < num++)
		putchar((num % 10) + '0');

	putchar('\n');

	return (0);
}
